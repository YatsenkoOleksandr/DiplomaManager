import { PredefensePeriod } from '../../shared/predefense-period.model';
import { PredefenseTeacherCapacity } from './predefense-teacher-capacity.model';

export class TeacherPredefensePeriod {
    predefensePeriod: PredefensePeriod;
    predefenseCapacity: PredefenseTeacherCapacity;
}