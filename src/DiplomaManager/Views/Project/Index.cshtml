@using DiplomaManager.ViewModels
@model ProjectViewModel

@{
    ViewBag.Title = "Подача заявки";
}

<h2>Подача заявки</h2>
<hr />

<my-app>Загрузка...</my-app>

@*<form asp-action="Create" asp-controller="Project" asp-anti-forgery="true">
    <div class="validation" asp-validation-summary="ModelOnly"></div>
    <div>
        <div class="form-group">
            <label asp-for="TeacherId"></label>
            <select asp-for="TeacherId" asp-items="@(new SelectList(ViewBag.Teachers, "Id", null))"
                    class="form-control"></select>
        </div>
        <div class="form-group">
            <label asp-for="DegreeId"></label>
            <select asp-for="DegreeId" asp-items="@(new SelectList(ViewBag.Degrees, "Id", "Name"))"
                    class="form-control"></select>
        </div>
        <div class="form-group">
            <label asp-for="DevelopmentAreaId"></label>
            <select asp-for="DevelopmentAreaId" asp-items="@(new SelectList(ViewBag.DevelopmentAreas, "Id", "Name"))"
                    class="form-control"></select>
        </div>
        <div class="form-group">
            <label asp-for="LastName"></label>
            <input type="text" asp-for="LastName" class="form-control" placeholder="Введите фамилию" />
            <span asp-validation-for="LastName" />
        </div>
        <div class="form-group">
            <label asp-for="FirstName"></label>
            <input type="text" asp-for="FirstName" class="form-control" placeholder="Введите имя" />
            <span asp-validation-for="FirstName" />
        </div>
        <div class="form-group">
            <label asp-for="Patronymic"></label>
            <input type="text" asp-for="Patronymic" class="form-control" placeholder="Введите отчество" />
            <span asp-validation-for="Patronymic" />
        </div>
        <div class="form-group">
            <label asp-for="Title"></label>
            <textarea type="text" asp-for="Title" class="form-control" placeholder="Введите тему работы"
                      rows="5"></textarea>
            <span asp-validation-for="Title" />
        </div>
        <div class="form-group">
            <input type="submit" value="Подать" class="btn btn-primary" />
        </div>
    </div>
</form>*@

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
